import { DefaultTheme } from "@react-navigation/native"

const visionaryTheme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        background: '#fff'
    }
}

export default visionaryTheme;